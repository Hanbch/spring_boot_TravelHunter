<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.hi.prj.mapper.BookingMapper">

    <insert id="booking">
           <![CDATA[
           		insert into booking(num, member_id, place_num, room_num, startdate, enddate, cname, cphone)
           		values(booking_seq.nextval, #{member_id}, #{place_num}, #{room_num}, #{startdate}, #{enddate}, #{cname}, #{cphone})
   			]]>
    </insert>
    
    <insert id="reserved">
           <![CDATA[
           		insert into reserved (room_num,rdate)
           		values(#{room_num}, to_date(#{rdate},'mm-dd-yy'))
   			]]>
    </insert>
    
</mapper>